# file_writer_component

## 功能
- 创建文件（支持自动创建父目录）
- 覆盖写入文件内容
- 追加写入文件内容
- 按文本片段替换文件内容

## 导出函数
- `component.handle.create_file`
- `component.handle.write_file`
- `component.handle.append_file`
- `component.handle.replace_file_text`

## 依赖
见同目录 `requirements.txt`（当前仅使用 Python 标准库）。
