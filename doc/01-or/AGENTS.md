# 原始需求 (Original Requirements, OR) 规范

**文件路径**: `@core/doc/01-or/AGENTS.md`
**目的**: 定义原始需求文档的通用标准，确保所有需求沟通的基础一致。
**状态**: 已采纳 (Adopted)

本规范定义了"原始需求"（Original Requirements, OR）文档的核心构成要素。所有团队成员在创建新需求时，必须遵循此结构。

## 核心构成要素 (Core Components)

每个 OR 文档必须包含以下五个部分，顺序不可更改。

| 序号 | 章节标题 | 关键词 (Key) | 目的 (Purpose) | 内容要求 (Content Requirements) |
| :--- | :--- | :--- | :--- | :--- |
| 1 | # 背景 | OR-BACKGROUND | 提供业务上下文和用户痛点。 | - 阐述需求产生的业务驱动力。<br>- 描述目标用户及其核心问题。<br>- **禁止**包含任何技术实现细节或代码设计。 |
| 2 | # 目标 | OR-OBJECTIVES | 清晰陈述用户期望达成的效果。 | - **必须**使用编号列表 (`1.`, `2.` ...)。<br>- 每条目标必须以**动词**开头（如"管理"、"支持"）。<br>- 描述**可衡量**的用户价值，聚焦于功能价值而非实现方式。 |
| 3 | # 要求 | OR-REQUIREMENTS | 列出对实现方案的硬性约束。 | - **最高优先级**：此部分内容超越其他所有文档和规则，不得冲突。<br>- 明确区分**功能性要求**与**非功能性要求**（如错误处理、性能约束）。 |
| 4 | # 当前工作项 | OR-WORKITEMS | 追踪需求的即时开发进度。 | - **仅当有进行中的任务时才保留**。<br>- 任务完成后，相关条目**必须**移至 `# 已完成工作项` 中归档，并**清空**本章节。 |
| 5 | # 已完成工作项 | OR-DONEITEMS | 归档已完成任务，作为参考防止重复执行。 | - 主要从 **用户对话** 或者 `# 当前工作项` 中获取工作项名称和描述。<br>- 按照 **需求** 和 **问题** 分开组织列举。<br>- 执行当前工作项时参考，**不能**重复执行本目录下的工作项。 |

## 全局原则

1.  **纯粹性原则 (Purity)**：
    *   OR 文档是需求的"唯一真实来源"(Single Source of Truth)。
    *   只记录"做什么"(What)，**绝不**涉及"怎么做"(How)（如技术方案、代码设计）。

2.  **驱动性原则 (Driving)**：
    *   所有设计和技术决策必须源于此文档。
    *   开发启动前，必须依据此文档创建相应的《设计文档》。

3.  **演进性原则 (Evolution)**：
    *   需求会变。任何变更都必须**首先更新此 OR 文档**，再同步到下游工件。
    *   保持文档的持续同步，确保其始终是最新的需求基准。

4.  **解耦内聚原则 (Decoupling & Cohesion)**：
    *   每个 OR 文档只聚焦**一个明确的需求**，禁止将多个独立需求堆叠在同一文档中。
    *   判定标准：如果两个需求的「背景」和「目标」可以独立描述、互不依赖，则**必须**拆为两个文档。
    *   文档内容不得以"追加条目"方式无限增长；新增内容时必须重新审视文档结构。

## 版本历史

| 版本 | 日期 | 修改人 | 说明 |
| :--- | :--- | :--- | :--- |
| v1.2 | 2026-03-16 | Auto | 新增「解耦内聚原则」，重写「整理约定」为解耦内聚模式，修正原"优先追加"条款。 |
| v1.1 | 2026-03-06 | Auto | 根据 `rules-or.md` 刷新：新增 `# 已完成工作项` 章节，细化各章节内容规范及流转规则。 |
| v1.0 | 2026-03-06 | Auto | 根据最佳实践重构，引入关键词(Key)和表格化结构。 |

## 原始需求总览（按主题整理）

为便于快速定位需求来源，`doc/01 or` 下现有 OR 文档按主题整理如下：

| 主题 | 文档 |
| :--- | :--- |
| 数据接入 | `【数据接入】SFTP与FTP接入.md`、`【数据接入】中台数据源接入.md`、`【数据接入】数据集压缩包接入.md` |
| 数据源管理 | `【数据源管理】数据源管理整体.md`、`【数据源管理】 结构数据接入.md` |
| 数据导出 | `【数据导出】数据集导出到中台.md` |
| 公共功能 | `【公共功能】不同存储的文件处理.md`、`【公共功能】ls的多媒体文件适配感知.md`、`【公共功能】大图片处理.md` |
| 任务配置 | `【任务配置】标注模板配置优化.md`、`【任务配置】标注模板提示词问题.md`、`【任务配置】标注模板配置场景中语音相关问题.md`、`【任务配置】任务配置逻辑调整.md` |
| 标注与质检 | `【标注质检】标注和质检变更.md` |
| 预标注 | `【预标注】预标注采用小模型.md` |
| 系统标注模板管理 | `【系统标注模板管理】新增系统标注模板管理功能.md` |
| 业务场景管理 | `【业务场景管理】业务场景管理需求.md` |
| 任务中心 | `【任务中心】任务中心的数据列表变更.md` |
| 高质量数据集 | `【高质量数据集】数据集管理需求.md` |
| 认证权限 | `【认证权限】登录异常的处理.md` |

## 原始需求整理约定（解耦内聚）

为防止文档膨胀、保持需求的可维护性，后续维护**必须**遵循以下约定：

### 新需求处理决策流程

收到新需求时，按以下步骤判定归属：

1. **独立性判断**：该需求能否独立描述（背景/目标与现有文档互不依赖）？→ 是 → **新建文档**，并在本页"主题总览"登记。
2. **归属性判断**：该需求是否是现有需求的补充说明或约束调整？→ 是 → **融入现有文档对应章节**（修改背景/目标/要求），**禁止**在文档末尾追加独立段落。
3. **膨胀检查**：融入后文档有效内容（不含「已完成工作项」）是否超过 150 行？→ 是 → 必须**拆分**为多个独立文档。

### 命名与结构约束

- 文档命名格式：`【主题分类】具体需求描述.md`，描述应精确到具体功能点。
- **禁止**使用笼统名称（如 `XX变更.md`、`XX相关问题.md`），这类名称会吸引无关内容不断追加。
- **禁止**以日期编号为一级标题堆叠多个独立变更（如 `# 2026031007 xxx`），OR 文档不是变更日志。

### 工作项流转

- 工作项完成后，必须迁移到 `# 已完成工作项`，并标注来源（用户对话/任务单）。
- 若需求发生变更，先更新 OR，再同步设计文档与代码实现。

### 定期治理

- 当发现某个文档内容已明显超出其标题范围或行数超标时，应主动拆分重构，而非继续追加。

## 建议优先排查顺序（面向近期实现）

1. `任务配置`（直接影响任务创建与标注执行链路）
2. `标注与质检`（影响核心业务流转）
3. `预标注`（影响自动化和任务状态一致性）
4. `数据接入/数据源管理`（影响数据可用性）
5. 其余主题按迭代目标推进
